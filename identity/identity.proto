syntax = "proto3";
package identity;

// Identity contains just enough information to identify a peer.
message Identity {
  // CertPem contains each certificate in the chain, with the first certificate as the leaf.
  repeated bytes cert_pem = 1;
}
