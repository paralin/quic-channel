package server

import (
// "github.com/lucas-clemente/quic-go"
)

// ServerConfig is the configuration for creating a Server.
type ServerConfig struct {
}

// Server manages sessions and handles listeners.
type Server struct {
}

// NewServer builds a new server.
func NewServer() (*Server, error) {
	return nil, nil
}
